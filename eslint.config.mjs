// ESLint flat config (ESLint 9/10 + Next 16).
// eslint-config-next 16 ships a complete flat-config array (core-web-vitals + typescript + ignores).
import next from 'eslint-config-next';

export default next;
